export { proposalFallback, toolResultRegistry } from "./registry";
export type {
    AgentProposalId,
    AgentThreadId,
    PartState,
    ProposalDiff,
    ProposalToolOutput,
    ProposeToolName,
    ReadToolName,
    RegistryEntry,
    ToolName,
    ToolOutput,
    ToolResultComponentProps,
} from "./types";
